openapi: 3.0.0

info:
  description: |
    # API 设计中心创建的 API 文档。
  
    请在『API 概况』中填写 API 文档的基本信息；在『API列表』新增接口描述；在『数据类型』中定义要引用的数据结构。
  title: dfef
  version: default
  
paths:
  /project:
    post:
      operationId: operationId
      requestBody:
        content:
          application/json:
            schema:
              example:
                propertyName1: Example
              properties:
                propertyName1:
                  example: Example
                  type: string
                  x-dice-name: propertyName1
                  x-dice-required: true
              required:
              - propertyName1
              type: object
              x-dice-media: application/json
      responses:
        "200":
          content:
            application/json:
              schema:
                example: {}
                type: object
                x-dice-media: application/json
                x-dice-name: schema
          description: ""
      tags:
      - other
  
components:
  {}
  
tags:
  - name: other
  