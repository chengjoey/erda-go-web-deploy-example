version: '2.0'
services:
  go-demo:
    ports:
      - port: 8080
        expose: true
    envs:
      ENV_A: A
    resources:
      cpu: 100
      mem: 200
    deployments:
      replicas: 1
    hosts:
      - 127.0.0.1 www.terminus.io
      - 127.0.0.2 dice.terminus.io
envs:
  ENV_FOO: FOO
addons:
  config:
    plan: config-center:basic
    options:
      version: 1.0.0
  go-demo-addon:
    # plan: config-center:basic
    # options:
    #   version: 1.0.0
